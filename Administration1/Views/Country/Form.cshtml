@using Application.Features.Country.Models
@model CountryDTO
@{
    Layout = "_Layout";
    var TableName = "Country";
   
    if (Model.Id > 0)
    {
    ViewData["Title"] = "Edit " +TableName + ":" + @Model.Name;
        <h1>@ViewData["Title"]</h1>
    } 
    
   
  else
  {
        ViewData["Title"] = "Add " + TableName;
        <h1>@ViewData["Title"]</h1>

  }
}

   <div asp-validation-summary="ModelOnly"></div>

<form asp-action="Form" class="pure-form" method="post"  enctype="multipart/form-data">
   
      

          
    <input type="hidden" asp-for="Id" />
    <div class="form-group col-md-6">
        Name
        <input asp-for="Name"/>
        <span asp-validation-for="Name"></span>
    </div>

    

    

     <Select asp-for="ParentId" asp-items="@ViewBag.Countries">

                <option selected="selected">Select</option>
                
            </Select>

    </div>


    

    <input type="submit" class="btn btn-primary btn-main-bg m-1" value="Save"/>
    <a href="@Url.ActionLink("index")" aria-expanded="false" class=" btn btn-primary bg-primary-700 text-white btn-icon rounded-circle mx-1 waves-effect waves-themed" style="font-size:large"><i class="fa fa-edit " data-toggle="tooltip" data-placement="top">Back</i></a>

</form>


<script>


 var nameInput = document.getElementById('ParentId');

document.querySelector('.pure-form').addEventListener('submit', function (e) {

    //e.preventDefault();
    console.log(nameInput.value);    
});

</script>